*

# Custom-Flipper Phase 2A tracked user apps (explicit exceptions to the
# blanket ignore above, added one at a time as each app is reviewed/imported)
!/network_subnet/
!/network_subnet/**
!/programmer_calc/
!/programmer_calc/**
!/vin_decoder/
!/vin_decoder/**
!/flipper95/
!/flipper95/**
!/chess/
!/chess/**

# Custom-Flipper Phase 2B tracked user apps (same explicit-exception pattern,
# added one at a time as each app is reviewed/imported)
!/flipfetch/
!/flipfetch/**
!/quadratic_solver/
!/quadratic_solver/**
!/sudoku/
!/sudoku/**

# Custom-Flipper Phase 2C tracked user apps (same explicit-exception pattern,
# added one at a time as each app is reviewed/imported)
!/sd_info/
!/sd_info/**
!/docviewlite/
!/docviewlite/**

# Custom-Flipper Phase 2D tracked user apps (same explicit-exception pattern,
# added one at a time as each app is reviewed/imported)
!/resistors/
!/resistors/**
!/crypto_dictionary/
!/crypto_dictionary/**
!/2048/
!/2048/**

# Custom-Flipper Phase 2E tracked user apps (same explicit-exception pattern,
# added one at a time as each app is reviewed/imported)
!/image_viewer/
!/image_viewer/**
!/boilerplate/
!/boilerplate/**
!/minesweeper/
!/minesweeper/**

# Custom-Flipper Phase 2F tracked user apps (same explicit-exception pattern,
# added one at a time as each app is reviewed/imported)
!/qrcode/
!/qrcode/**
