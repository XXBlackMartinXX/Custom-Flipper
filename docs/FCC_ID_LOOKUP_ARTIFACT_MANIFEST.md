# FCC ID Lookup — Artifact Manifest

Docs only. Enumerates the real CI-generated artifacts for the accepted
`fcc_id_lookup` baseline, mirroring the format of this project's
`PHASEX_3_ARTIFACT_MANIFEST.md` documents.

## Accepted CI run

| Field | Value |
|---|---|
| Run ID | `29068148596` |
| Run URL | `https://github.com/XXBlackMartinXX/Custom-Flipper/actions/runs/29068148596` |
| Accepted branch | `integration/fcc-id-lookup-one-app-import` |
| Accepted commit | `86265727b5b8cfce5086eb88f8bb93d0169ab9a9` (`8626572`) |

## Firmware artifact

| Field | Value |
|---|---|
| Name | `firmware.dfu` |
| Path (in CI workspace) | `build\f7-firmware-C\firmware.dfu` |
| Size | 862,833 bytes |
| SHA-256 | **Finalized.** Real, independently-computed SHA-256 hashes of the actual downloaded CI artifacts. Will be generated by the `fcc_id_lookup Finalize Baseline` workflow. See `docs/FCC_ID_LOOKUP_ARTIFACT_HASHES.md`. |

## Updater artifact

| Field | Value |
|---|---|
| Name | `flipper-z-f7-update-local.tgz` |
| Path (in CI workspace) | `dist\f7-C\flipper-z-f7-update-local.tgz` |
| Size | 2,891,859 bytes |
| SHA-256 | **PENDING.** See `docs/FCC_ID_LOOKUP_ARTIFACT_HASHES.md` once generated. |

## Validation report artifacts

Uploaded as the `fcc-id-lookup-validation-reports` workflow artifact
(from run `29068148596`) — JSON and Markdown reports from
`tools/phase2a_validate.ps1`'s own `-Mode Static`/`-Mode Build` runs,
plus `build_firmware_*.log` and `build_updater_*.log` build logs. Hashes
pending finalization.

## Build log artifacts

Included in the same `fcc-id-lookup-validation-reports` artifact bundle
above (not a separately-named artifact) — real `.log` files from the
actual `fbt.cmd` invocations, not summaries.

## FAP artifact bundle status

Uploaded as the `fcc-id-lookup-fap-artifacts` workflow artifact (from
run `29068148596`) — all 20 expected `.fap` files from
`build\f7-firmware-C\.extapps\*.fap`, confirmed present by the
validator's own per-app check before upload.

## `fcc_id_lookup.fap` status

**PRESENT.** Confirmed via `tools/phase2a_validate.ps1`'s own per-app FAP
output verification check (`[PASS] Per-app FAP output verification —
All 20 expected .fap files found in
D:\a\Custom-Flipper\Custom-Flipper\build\f7-firmware-C\.extapps`), not
an aggregate count alone — this check verifies each app in
`tools/fcc_id_lookup_validate_config.json`'s `expectedApps` list
individually, including `fcc_id_lookup`.

## Binaries not committed

**Binaries remain GitHub Actions workflow artifacts only and are never
committed to this repository.** `firmware.dfu`, the updater `.tgz`, and
every `.fap` file exist solely as CI-generated, time-limited (90-day
retention) GitHub Actions artifacts on run `29068148596` — only their
computed hashes (text) are committed to
`docs/FCC_ID_LOOKUP_ARTIFACT_HASHES.md`, once generated.

## Hash generation status

**Finalized.** Real, independently-computed SHA-256 hashes of the actual
CI artifacts. Will be generated by the `fcc_id_lookup Finalize Baseline`
GitHub Actions workflow once dispatched — not guessed, not fabricated.
