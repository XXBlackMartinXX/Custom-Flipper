# FCC ID Lookup — Baseline Acceptance Record

Docs only. This is the closing acceptance document for the dedicated
`fcc_id_lookup` one-app import CI baseline, mirroring the format of this
project's `PHASEX_3_ACCEPTANCE_RECORD.md` documents.
`.github/workflows/fcc-id-lookup-finalize-baseline.yml` has now actually
run — see "Finalization workflow result" below for the real, completed
result.

## Baseline-selection check (performed before finalizing)

Before finalizing, the true current HEAD of
`integration/fcc-id-lookup-one-app-import` was checked against real CI
run history, not assumed. At the time this finalization was prepared,
HEAD was commit `86265727b5b8cfce5086eb88f8bb93d0169ab9a9` (`8626572`,
"fcc: document import validation (part 2 - real CI build result)") — one
commit past `ff5a69ba05e1d7dd1d5876155f90696105f9e92d` (the commit CI run
`29067243595` validated). A later, automatically-triggered CI run
([`29068148596`](https://github.com/XXBlackMartinXX/Custom-Flipper/actions/runs/29068148596))
exists at that exact HEAD commit and independently confirmed `conclusion:
success`, verified via `get_workflow_run`/`list_workflow_jobs`/
`get_job_logs`, not claimed on trust. Per this phase's own
baseline-selection rule, **this later run is used as the accepted
baseline instead of `29067243595`** — the earlier run's own evidence
remains valid and preserved in `docs/FCC_ID_LOOKUP_BUILD_REPORT.md`, not
erased, just superseded as the baseline reference.

## Final classification: **FCC_ID_LOOKUP ACCEPTED FOR NON-HARDWARE CI BASELINE ONLY**

The CI Windows validation run (`29068148596`) succeeded, with conclusion
verified via the GitHub API, not claimed on trust. This acceptance
record is locked in against that real run, at the actual branch HEAD
that was current when finalization was prepared. Artifact hash
finalization and tag creation were handled by the
`fcc_id_lookup Finalize Baseline` workflow — see "Finalization workflow
result" below for the real, completed result.

## Branch

`integration/fcc-id-lookup-one-app-import`

## Accepted commit

`86265727b5b8cfce5086eb88f8bb93d0169ab9a9` (`8626572`)

## Accepted CI run

| Field | Value |
|---|---|
| Run ID | [`29068148596`](https://github.com/XXBlackMartinXX/Custom-Flipper/actions/runs/29068148596) |
| Run URL | `https://github.com/XXBlackMartinXX/Custom-Flipper/actions/runs/29068148596` |
| CI conclusion | `success`, independently verified via `get_workflow_run`/`list_workflow_jobs`/`get_job_logs` |
| Static validation result | `PASS_WITH_REVIEWED_FALSE_POSITIVES` |
| Build validation result | `PASS` |
| `updater_package` result | `PASS` (exit code 0) |
| `fcc_id_lookup.fap` verification | `PASS` — confirmed present via the validator's own per-app FAP output check |
| Total expected custom FAP count | 20 (19 already-accepted Phase 2A-2F apps + `fcc_id_lookup`), all 20 confirmed present |

## Imported app

`fcc_id_lookup` — appid `fcc_id_lookup`, path
`applications_user/fcc_id_lookup/`, real appid confirmed unique and
matching `application.fam`.

## Import commit

`579b355` ("fcc: import fcc_id_lookup")

## License attribution status

**Preserved.** The confirmed upstream MIT `LICENSE` (Copyright (c) 2026
lsr, `lrehmann/fcc-id-lookup-flipper`) was added byte-verbatim alongside
the app's own source at import time, independently confirmed to match
the canonical MIT template exactly before use. Full chain of evidence in
`docs/FCC_ID_LOOKUP_LICENSE_RESOLUTION.md` and
`docs/FCC_ID_LOOKUP_LICENSE_ATTRIBUTION.md`.

## Database-not-bundled status

**Confirmed.** No FCC frequency/applicant database file (`.bin` or
otherwise) is bundled, committed, or staged anywhere in this repository.
The database remains a separate, optional, user-sourced download per the
app's own `README.md` and source-level fallback behavior.

## Storage behavior

**Read-only, app-scoped.** All 11 storage-related call sites in
`fcc_id_lookup.c` are read/open/close/free lifecycle operations; the
single `storage_file_open` call uses `FSAM_READ`/`FSOM_OPEN_EXISTING`
only. Zero write-capable calls anywhere in the source. Full detail in
`docs/FCC_ID_LOOKUP_SAFETY_REVIEW.md`.

## Safety scan summary

Real static scan via `tools/phase2a_validate.ps1 -Mode Static
-ConfigPath tools/fcc_id_lookup_validate_config.json`, run both locally
and in CI: `PASS_WITH_REVIEWED_FALSE_POSITIVES`. Zero high-confidence
unsafe API matches (no RF/Sub-GHz/NFC/RFID/iButton/BadUSB/HID/GPIO/IR).
11 generic-keyword matches newly attributable to `fcc_id_lookup`, all
individually reviewed and confirmed benign. Zero network/HTTP behavior,
zero credential/token/API-key handling. Full detail in
`docs/FCC_ID_LOOKUP_SAFETY_REVIEW.md`.

## Artifact paths, sizes, and hashes

| Field | Value |
|---|---|
| Artifact hashes generated | **YES** — generated by the `fcc_id_lookup Finalize Baseline` GitHub Actions workflow, run [`29096377711`](https://github.com/XXBlackMartinXX/Custom-Flipper/actions/runs/29096377711). See `docs/FCC_ID_LOOKUP_ARTIFACT_HASHES.md`. |

See "Finalization workflow result" below for the real, current status —
this section is not duplicated here to avoid two sources of truth
drifting apart.

## Limitations

- **No hardware flashing/testing performed.** No `-Mode HardwareAssisted`
  invocation exists anywhere in this phase's workflow or tooling.
- **No GUI/manual smoke test performed.** No human has observed
  `fcc_id_lookup` running on a real device screen.
- **Not release-ready.**
- **TEST-READY ONLY.**

## Final classification: **FCC_ID_LOOKUP ACCEPTED FOR NON-HARDWARE CI BASELINE ONLY**

See "Finalization workflow result" below for the real, completed result
and the resulting next-gate determination.

---

## Finalization workflow result

**Run.** The `fcc_id_lookup Finalize Baseline` workflow
([`29096377711`](https://github.com/XXBlackMartinXX/Custom-Flipper/actions/runs/29096377711))
was dispatched against `target_branch=integration/fcc-id-lookup-one-app-import`
with `source_run_id=29068148596`,
`ci_baseline_sha=86265727b5b8cfce5086eb88f8bb93d0169ab9a9`,
`acceptance_tag_name=fcc-id-lookup-acceptance-record-20260710`,
`ci_tag_name=fcc-id-lookup-ci-baseline-20260710`, and completed with
**all 13 steps `conclusion: success`**, independently confirmed via
`list_workflow_jobs` and `get_job_logs` (not claimed on trust).

**Artifacts downloaded and hashed** (real `Get-FileHash -Algorithm
SHA256` on the GitHub-hosted Windows runner, from `source_run_id`
`29068148596`'s own uploaded artifacts via `gh run download`):

| File | Size (bytes) | SHA-256 |
|---|---|---|
| `firmware.dfu` | 862,833 | `e8c11b62429677e727a42682b2e7f2bf8eaf8e5a84e8887f2d3db6b137328f1d` |
| `flipper-z-f7-update-local.tgz` | 2,891,859 | `eec5b148892a3d89c724006bd082f1ca083e05990aa7b8cad43868bf8347cc55` |
| `fcc_id_lookup.fap` | 20,196 | `168025ddcffb01f94e1af8eefcead2ac80d69f6b7ad9856a603e1a7d30317658` |

Full validation-report, build-log, and all-42-`.fap` hash tables are in
`docs/FCC_ID_LOOKUP_ARTIFACT_HASHES.md`.

**Docs commit.** The workflow committed the patched
`docs/FCC_ID_LOOKUP_ARTIFACT_HASHES.md` and
`docs/FCC_ID_LOOKUP_ARTIFACT_MANIFEST.md` (plus this document's own
"Artifact hashes generated" line) as commit `1c0232b`
("docs: finalize fcc_id_lookup artifact hashes from CI artifacts", 3
files changed) under the `github-actions[bot]` identity, pushed to
`integration/fcc-id-lookup-one-app-import`, and pulled into this local
checkout — this closing section is the one manual edit made on top of
that automated commit, since the workflow's sentinel-replacement step
does not touch this section by design.

**Tags created**, independently re-verified via a fresh `git ls-remote
--tags origin` (not just trusted from the workflow's own log):

| Tag | Target commit | Verified |
|---|---|---|
| `fcc-id-lookup-ci-baseline-20260710` | `86265727b5b8cfce5086eb88f8bb93d0169ab9a9` | ✅ dereferences correctly |
| `fcc-id-lookup-acceptance-record-20260710` | `1c0232b4137c366c5b79f651136b3097abf69a69` | ✅ dereferences correctly |

All 10 prior Phase 2A–2F tags (`phase2a-*` through `phase2f-*`)
confirmed **unchanged** at their original target commits — zero
regression to any prior phase's baseline.

**Final classification: FCC_ID_LOOKUP ACCEPTED FOR NON-HARDWARE CI
BASELINE ONLY.** No hardware testing performed. Not release-ready. Next
allowed gate: hardware-assisted validation and/or a future broader
import batch, each a separate, explicitly-requested step — neither
started here.
