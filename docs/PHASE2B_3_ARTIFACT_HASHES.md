# Phase 2B — Local Artifact Hash Manifest

**Status: NOT YET GENERATED.** This document will be overwritten by the
`Phase 2B Finalize Baseline` GitHub Actions workflow
(`.github/workflows/phase2b-finalize-baseline.yml`) once it runs. Until
then, no hash value below is fabricated — this file states plainly that
finalization is pending, the same discipline Phase 2A's own
`PHASE2A_ARTIFACT_HASHES.md` used between Phase 2A.10 (blocked) and
Phase 2A.11 (finalized).

## Source

| Field | Value |
|---|---|
| Source CI run ID | `28877810474` |
| Source CI run URL | https://github.com/XXBlackMartinXX/Custom-Flipper/actions/runs/28877810474 |
| CI baseline commit (SHA) | `50dfe2fadb2e587f4e8ed67edbf7f60e42b90159` |
| Hashes generated by | *(pending — will be filled in with the `Phase 2B Finalize Baseline` workflow run ID once it executes)* |

## Firmware and updater artifacts

| File | Size (bytes, per CI job log) | SHA-256 |
|---|---|---|
| `firmware.dfu` | 862,825 | *PENDING* |
| `flipper-z-f7-update-local.tgz` | 2,742,659 | *PENDING* |

## Validation reports and build logs

| File | Size (bytes) | SHA-256 |
|---|---|---|
| *(pending — populated by the finalization workflow after downloading `phase2b-validation-reports`)* | | |

## Why this is pending

This session's own network path cannot download GitHub Actions artifacts
directly — they always redirect to Azure Blob Storage
(`productionresultssa*.blob.core.windows.net`), and this session's egress
policy returns a confirmed `403` for that host (verified directly via
`curl` in this same phase). This is the identical, already-documented
limitation from Phase 2A.10 — not a new problem, not specific to Phase
2B's artifacts.

## What this does and does not mean

- **This is not a fabricated placeholder disguised as a hash** — every
  value above that isn't yet known is marked `PENDING`, not filled with a
  guessed or copied value.
- **No binary is or will be committed to this repository.**
  `firmware.dfu` and the updater `.tgz` exist only as GitHub Actions
  workflow artifacts on the source run; only their hashes (text) will be
  committed here once generated.
- **Hardware-assisted validation: NOT RUN. Hardware flashing/testing: NOT
  PERFORMED.**
- **Release status: TEST-READY ONLY / NOT RELEASE-READY.** Generating
  these hashes will not change either of those.
