# Phase 2C — CI Artifact Manifest

Docs only. This records exactly which GitHub Actions artifacts back the
Phase 2C acceptance record (`docs/PHASE2C_3_ACCEPTANCE_RECORD.md`), where
to find them, and how to independently verify them by hashing the actual
files after downloading — none of these binaries are committed to this
repository.

## Source run

| Field | Value |
|---|---|
| CI run ID | `28897702247` |
| CI run URL | https://github.com/XXBlackMartinXX/Custom-Flipper/actions/runs/28897702247 |
| Head SHA | `969054ee9f802f72be1064a62052c4be82a91783` |
| Branch | `integration/phase2c-first-batch` |
| Workflow | Phase 2C Windows Validation |

## Artifacts (as reported by the GitHub Actions API for this run)

| Artifact name | Artifact ID | Archive (zip) size | Archive digest (GitHub-computed) | Expires |
|---|---|---|---|---|
| `phase2c-firmware-artifacts` | `8151182968` | 3,353,122 bytes | `sha256:7460c3424679b8b1c5a984f5bbfd5ba31204a017feb6123885e7b967d7dea800` | 2026-10-05 |
| `phase2c-validation-reports` | `8151182167` | 38,402 bytes | `sha256:41f3c0c7f868b7268ac4876e4ca6732d00006d8e07c4e8d581f326f060006f22` | 2026-10-05 |

**Important**: the digests above are GitHub's own hash of the **zip
archive** it generated for artifact download — they are not, and should
not be treated as, hashes of the individual files inside. To get a hash of
`firmware.dfu` or the updater `.tgz` themselves, download and extract the
artifact, then hash each file directly (see "Firmware/updater SHA-256"
below — generated by `.github/workflows/phase2c-finalize-baseline.yml`,
the same mechanism Phase 2A.11/2B.3 used).

## Contents of `phase2c-firmware-artifacts`

| File | Path inside artifact | Size |
|---|---|---|
| Firmware | `build/f7-firmware-C/firmware.dfu` | 862,825 bytes |
| Updater package | `dist/f7-C/flipper-z-f7-update-local.tgz` | 2,757,340 bytes |

## Contents of `phase2c-validation-reports`

Per `.github/workflows/phase2c-windows-validation.yml`'s upload step,
this artifact contains everything written under `reports\phase2c\`
during the run: the timestamped Static-mode JSON + Markdown reports, the
timestamped Build-mode JSON + Markdown reports, and the raw `fbt.cmd`
build/updater log files captured during the Build step.

## Firmware/updater SHA-256

**PENDING.** Real, independently-computed SHA-256 hashes of the actual
`firmware.dfu` and updater `.tgz` files (not the zip-archive digests
above) require downloading and extracting the artifact — this session's
own network path cannot do that directly (GitHub Actions artifact
downloads always redirect to Azure Blob Storage, which this session's
egress policy blocks with a confirmed `403`, the same limitation
documented since Phase 2A.10). `.github/workflows/phase2c-finalize-baseline.yml`
resolves this the same way Phase 2A.11/2B.3 did: running entirely on
GitHub's own Windows infrastructure, downloading the artifacts directly
via `gh run download`, and hashing them with `Get-FileHash`. See
`docs/PHASE2C_3_ARTIFACT_HASHES.md` for the real values once that
workflow has run.

## Where these artifacts live

**These artifacts are stored only as GitHub Actions workflow artifacts
tied to run `28897702247` — they are not, and will not be, committed as
binaries to this repository**, consistent with this project's standing
policy against committing `build/`, `dist/`, or `toolchain/` output.
GitHub retains workflow artifacts for a limited window (90 days, per the
workflow's own `retention-days: 90` setting; the API reports an explicit
expiry of 2026-10-05 for both artifacts above) — after that, they are
deleted by GitHub regardless of anything in this repository.

## Validating a real device flash later

Once a physical Flipper Zero and Windows machine are available, a
Phase-2C-pointing hardware gate (modeled on
`tools/phase2b_hardware_gate.ps1`/`tools/phase2b_hardware_gate_config.json`,
not created in this phase since no device or Windows machine is available
to use it against here) could verify a locally-downloaded copy of these
two files against the real hashes in `docs/PHASE2C_3_ARTIFACT_HASHES.md`
before any flash is considered — the same discipline already used for the
Phase 2A/2B baselines.
