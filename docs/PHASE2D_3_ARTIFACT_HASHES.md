# Phase 2D — Local Artifact Hash Manifest

**Status: PENDING.** This file will be generated/overwritten by the
`Phase 2D Finalize Baseline` GitHub Actions workflow once it runs
successfully. No hash is fabricated here — this is an honest placeholder,
not a guessed or pre-filled value.

## Source

| Field | Value |
|---|---|
| Source CI run ID | `28941093859` |
| Source CI run URL | https://github.com/XXBlackMartinXX/Custom-Flipper/actions/runs/28941093859 |
| CI baseline commit (SHA) | `d0812638a02c50389b9e713ad98f2c8215b75dd5` |
| Hashes generated by | *(pending — will be the `Phase 2D Finalize Baseline` workflow run)* |

## Firmware and updater artifacts

| File | Size (bytes) | SHA-256 |
|---|---|---|
| `firmware.dfu` | *(pending)* | *(pending)* |
| `flipper-z-f7-update-local.tgz` | *(pending)* | *(pending)* |

## Validation reports and build logs

*(pending)*

## `.fap` artifact bundle

*(pending)*

## What this will and will not mean once generated

- These will be real hashes of the actual downloaded files, computed with
  PowerShell's `Get-FileHash -Algorithm SHA256` on a GitHub-hosted Windows
  runner that downloaded them directly from the source CI run via
  `gh run download`.
- No binary will be committed to this repository. `firmware.dfu` and the
  updater `.tgz` exist only as GitHub Actions workflow artifacts on the
  source run; only their hashes (text) will be committed here.
- **Hardware-assisted validation: NOT RUN. Hardware flashing/testing: NOT
  PERFORMED.**
- **Release status: TEST-READY ONLY / NOT RELEASE-READY.** Generating
  these hashes will not change either of those.
