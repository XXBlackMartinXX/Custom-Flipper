# Phase 2F - Local Artifact Hash Manifest

**Status: PENDING.** This file will contain real, independently-computed
SHA-256 hashes of the actual Phase 2F CI artifacts, generated by the
GitHub Actions "Phase 2F Finalize Baseline" workflow running on a real
GitHub-hosted `windows-latest` runner — not fabricated, not copied from
any archive-level digest. **No hash value exists in this file until that
workflow has actually run and computed it for real.**

## Source (known now, before finalization)

| Field | Value |
|---|---|
| Source CI run ID | `29017861599` |
| Source CI run URL | https://github.com/XXBlackMartinXX/Custom-Flipper/actions/runs/29017861599 |
| CI baseline commit (SHA) | `37d11cada5a83afdeb752c6b2106216d7fc09b9f` |
| Hashes generated by | *(pending — will be the "Phase 2F Finalize Baseline" workflow run ID once dispatched)* |

## Firmware and updater artifacts

**PENDING.** Will contain `firmware.dfu` and
`flipper-z-f7-update-local.tgz`, each with real size and SHA-256, once
`.github/workflows/phase2f-finalize-baseline.yml` has run.

## Validation reports and build logs

**PENDING.**

## `.fap` artifact bundle

**PENDING.**

## What this will mean once generated

- These will be real hashes of the actual downloaded files, computed with
  PowerShell's `Get-FileHash -Algorithm SHA256` on a GitHub-hosted Windows
  runner that downloaded them directly from the source CI run via
  `gh run download`.
- No binary is or will be committed to this repository. `firmware.dfu`
  and the updater `.tgz` exist only as GitHub Actions workflow artifacts
  on the source run; only their hashes (text) are committed here.
- **Hardware-assisted validation: NOT RUN. Hardware flashing/testing: NOT
  PERFORMED.**
- **Release status: TEST-READY ONLY / NOT RELEASE-READY.** Generating
  these hashes does not change either of those.
