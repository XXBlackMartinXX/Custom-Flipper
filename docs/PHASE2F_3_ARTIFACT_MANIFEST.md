# Phase 2F — CI Artifact Manifest

Docs only. This records exactly which GitHub Actions artifacts back the
Phase 2F acceptance record (`docs/PHASE2F_3_ACCEPTANCE_RECORD.md`), where
to find them, and how to independently verify them by hashing the actual
files after downloading — none of these binaries are committed to this
repository.

## Source run

| Field | Value |
|---|---|
| CI run ID | `29017861599` |
| CI run URL | https://github.com/XXBlackMartinXX/Custom-Flipper/actions/runs/29017861599 |
| Head SHA | `37d11cada5a83afdeb752c6b2106216d7fc09b9f` |
| Branch | `integration/phase2f-first-batch` |
| Workflow | Phase 2F Windows Validation |
| Job | `Static + Build validation (windows-latest)` (job `86117245023`), conclusion `success` |

## Artifacts (as reported by the GitHub Actions API for this run)

| Artifact name | Artifact ID | Archive (zip) size | Archive digest (GitHub-computed) | Expires |
|---|---|---|---|---|
| `phase2f-firmware-artifacts` | `8198484143` | 3,474,281 bytes | `sha256:1832ed9b3b85416a1429a475a571191911d871a647556a865bf76b9b07f4838e` | 2026-10-07 |
| `phase2f-validation-reports` | `8198483772` | 50,701 bytes | `sha256:4e01fe87e84b830d2bc379f5ab4d4612f00270046f4222d6f63ed50fb1d547e9` | 2026-10-07 |
| `phase2f-fap-artifacts` | `8198484429` | 666,835 bytes | `sha256:6055181e4310eabc6f04165d8947436916100f9078e058b5fd5a5471a62d8321` | 2026-10-07 |

**Important**: the digests above are GitHub's own hash of the **zip
archive** it generated for artifact download — they are not, and should
not be treated as, hashes of the individual files inside. To get a hash
of `firmware.dfu` or the updater `.tgz` themselves, download and extract
the artifact, then hash each file directly (see "Firmware/updater
SHA-256" below — generated by
`.github/workflows/phase2f-finalize-baseline.yml`, the same mechanism
Phase 2A.11/2B.3/2C.3/2D.3/2E.3 used).

## Contents of `phase2f-firmware-artifacts`

| File | Path inside artifact | Size |
|---|---|---|
| Firmware | `build/f7-firmware-C/firmware.dfu` | 862,825 bytes |
| Updater package | `dist/f7-C/flipper-z-f7-update-local.tgz` | 2,878,428 bytes |

## Contents of `phase2f-validation-reports`

Per `.github/workflows/phase2f-windows-validation.yml`'s upload step,
this artifact contains everything written under `reports\phase2f\`
during the run: the timestamped Static-mode JSON + Markdown reports, the
timestamped Build-mode JSON + Markdown reports, and the raw `fbt.cmd`
build/updater log files captured during the Build step.

## Contents of `phase2f-fap-artifacts`

Per-app `.fap` build outputs from `build/f7-firmware-C/.extapps/*.fap` —
19 files total: `qrcode.fap`, `hex_viewer.fap`, `barcode_app.fap` (the 3
Phase 2F apps), plus the 16 already-accepted Phase 2A/2B/2C/2D/2E apps'
`.fap` files. This artifact uploads correctly via the Phase 2D.2
`include-hidden-files: true` fix, inherited unchanged.

## Firmware/updater SHA-256

**PENDING.** Real, independently-computed SHA-256 hashes of the actual
`firmware.dfu` and updater `.tgz` files (not the zip-archive digests
above) require downloading and extracting the artifact — this session's
own network path cannot do that directly (GitHub Actions artifact
downloads always redirect to Azure Blob Storage, which this session's
egress policy blocks with a confirmed `403`, the same limitation
documented since Phase 2A.10; re-confirmed repeatedly throughout this
entire project, including multiple times during the Phase 2F.2A
investigation).
`.github/workflows/phase2f-finalize-baseline.yml` resolves this the same
way Phase 2A.11/2B.3/2C.3/2D.3/2E.3 did: running entirely on GitHub's own
Windows infrastructure, downloading the artifacts directly via
`gh run download`, and hashing them with `Get-FileHash`. See
`docs/PHASE2F_3_ARTIFACT_HASHES.md` for the real values once that
workflow has run. **No value below or in that file is fabricated in
advance.**

## Where these artifacts live

**These artifacts are stored only as GitHub Actions workflow artifacts
tied to run `29017861599` — they are not, and will not be, committed as
binaries to this repository**, consistent with this project's standing
policy against committing `build/`, `dist/`, or `toolchain/` output.
GitHub retains workflow artifacts for a limited window (90 days, per the
workflow's own `retention-days: 90` setting; the API reports an explicit
expiry of 2026-10-07 for all three artifacts above) — after that, they
are deleted by GitHub regardless of anything in this repository.

## Validating a real device flash later

Once a physical Flipper Zero and Windows machine are available, a
Phase-2F-pointing hardware gate (modeled on
`tools/phase2e_hardware_gate.ps1`/`tools/phase2e_hardware_gate_config.json`,
not created in this phase since no device or Windows machine is available
to use it against here) could verify a locally-downloaded copy of these
two files against the real hashes in `docs/PHASE2F_3_ARTIFACT_HASHES.md`
before any flash is considered — the same discipline already used for the
Phase 2A/2B/2C/2D/2E baselines.
